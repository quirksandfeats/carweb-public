#!/usr/bin/env python3
"""SUPERSEDED: this app moved from Ollama to llama.cpp (real user request).
This file couldn't be deleted from this environment, so it's left behind as
a pointer rather than removed -- delete it yourself if you'd like it gone.
Use qa/stub_llama_server.py instead.
"""
import sys

if __name__ == "__main__":
    print("This app no longer uses Ollama -- it now runs on llama.cpp (llama-server).", file=sys.stderr)
    print("Use qa/stub_llama_server.py instead.", file=sys.stderr)
    sys.exit(1)
